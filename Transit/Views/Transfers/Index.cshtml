@model IEnumerable<Transit.Models.Transfer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fromStop.code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.toStop.code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.type.label)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fromStop.code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.toStop.code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.type.label)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
